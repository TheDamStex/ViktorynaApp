<Window x:Class="ViktorynaApp.MyResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Мої результати" Height="500" Width="700" Background="#F0F4FF">

    <Border Margin="20" CornerRadius="15" Background="White" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0"
                       Text="Мої результати" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"/>

            <!-- Проста статистика -->
            <Border Grid.Row="1" 
                    BorderBrush="#4CAF50" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="15" 
                    Margin="0,0,0,15"
                    Background="#F1F8E9">
                <StackPanel>
                    <TextBlock Text="Статистика" 
                               FontWeight="SemiBold"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding ZagalnaKilkistViktoryn, StringFormat='Вікторин пройдено: {0}'}"/>
                    <TextBlock Text="{Binding ZagalnaKilkistPravylnyh, StringFormat='Правильних відповідей: {0}'}"/>
                    <TextBlock Text="{Binding SeredniyProcent, StringFormat='Середній результат: {0:F1}%'}"/>
                </StackPanel>
            </Border>

            <!-- Список результатів -->
            <Border Grid.Row="2" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Margin="0,0,0,15">
                <ScrollViewer>
                    <ListView ItemsSource="{Binding Rezultaty}"
                              BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Розділ" Width="150" DisplayMemberBinding="{Binding Rozdil}"/>
                                <GridViewColumn Header="Результат" Width="120" DisplayMemberBinding="{Binding RezultatText}"/>
                                <GridViewColumn Header="Дата" Width="200" DisplayMemberBinding="{Binding DataVykonannia}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </ScrollViewer>
            </Border>

            <!-- Кнопка -->
            <Button Grid.Row="3"
                    Content="Назад" 
                    Height="35" 
                    Width="120"
                    HorizontalAlignment="Center"
                    Click="Back_Click"/>
        </Grid>
    </Border>
</Window>
